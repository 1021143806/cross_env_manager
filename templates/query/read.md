# 跨环境任务分析查询页面

## 📌 概述

本页面是 **AGV 跨环境任务下发系统** 的独立深度查询模块，旨在提供一个统一、强大的任务分析入口。它支持通过**任务单号**、**设备号**或**货架号**进行多维度查询，并能自动聚合跨服务器、跨数据库的任务数据，以清晰的结构化视图呈现，帮助运维与开发人员快速定位问题、分析任务链路。

## 🎯 核心功能

| 功能模块               | 描述                                                                                                                                 |
| :--------------------- | :----------------------------------------------------------------------------------------------------------------------------------- |
| **三模式查询**         | 支持 `任务单号 (OrderId)` 精确查询、`设备号` 深度查询（含最近5条任务切换）、`货架号` 智能查询（按状态优先级自动定位最近任务）。        |
| **设备实时状态**       | 三种查询方式均自动展示设备实时状态卡片（各服务器上的设备状态、电量、区域等）。                                                        |
| **IP 智能输入**        | 支持输入完整 IP（如 `10.68.2.32`）或仅最后一段数字（如 `32`），系统自动补全为 `10.68.2.32`。                                           |
| **本地数据库关联**     | 可选从本地数据库查询 `task_group` 及 `task_group_detail` 表的完整记录，并与远程任务数据并排展示。                                     |
| **模板关联与跳转**     | 自动解析任务关联的大任务模板及子任务模板，并提供 **一键跳转到模板编辑页面** 的快捷入口。                                              |
| **跨服务器子任务聚合** | 根据主任务详情，并发请求各子任务所在的服务器，将分散的子任务状态汇总为统一表格（Markdown 风格渲染）。                                 |
| **同模板任务监控**     | 列出该大模板下**其他执行中/异常状态**的任务清单，便于评估整体负载与冲突。                                                              |
| **原始报文查看**       | 所有接口返回的数据均可通过「查看原始 JSON」按钮在模态框中格式化展示，支持一键复制，便于调试与问题上报。                                 |
| **查询历史记录**       | 自动保存最近 5 次查询关键词（基于 LocalStorage），点击标签可快速回填并重新查询。                                                        |
| **任务重发**           | 支持对非执行中/已完成的任务进行重发，流程含设备预占→取消任务→修改数据库，前端渐进刷新 2s→3s→4s。                                      |

## 🏗️ 系统架构图

```mermaid
flowchart TB
    subgraph 用户界面
        A[查询面板]
        B[结果展示区]
        C[JSON详情模态框]
    end

    subgraph 前端逻辑层
        D[查询调度器]
        E[IP智能解析]
        F[状态优先级轮询]
        G[数据聚合与渲染]
        H[历史记录管理]
        I[最近任务标签栏]
    end

    subgraph 后端与数据源
        J["目标AGV服务器\n(如 10.68.2.32:8315)"]
        K[本地业务数据库\nMySQL]
        L[模板管理后端]
    end

    A -- 输入任务单号/设备号/货架号 --> D
    E -- 处理IP简写 --> D
    D -- 设备号查询 --> F
    D -- 货架查询 --> F
    F -- 轮询不同状态接口 --> J
    J -- 返回主任务/子任务详情 --> D
    D -- 可选查询 --> K
    K -- task_group / task_group_detail --> D
    D -- 模板信息请求 --> L
    L -- 模板结构数据 --> D
    D -- 聚合结果 --> G
    G -- 渲染HTML --> B
    B -- 点击JSON按钮 --> C
    H -- 读写LocalStorage --> A
    I -- 点击切换任务 --> D
```

## 🔄 查询流程

```mermaid
flowchart LR
    subgraph 设备号查询
        A1[输入设备号] --> A2[step1: pageSize=5\n查最近任务]
        A2 --> A3[返回 recent_tasks\n+ 第一条深度结果]
        A3 --> A4[渲染任务标签栏\n+ 第一条详情]
        A4 --> A5{点击其他任务}
        A5 --> A6[调用 device_task_detail\n获取深度结果]
        A6 --> A4
    end

    subgraph 任务单号查询
        B1[输入orderId] --> B2[调用 order_tasks API]
        B2 --> B3[step2~4: 查主任务\n+ 子任务 + 设备状态]
        B3 --> B4[渲染结果\n含设备状态卡片]
    end

    subgraph 货架号查询
        C1[输入货架号] --> C2[按状态优先级\n查找orderId]
        C2 --> C3[调用 order_tasks API]
        C3 --> C4[渲染结果\n含设备状态卡片]
    end
```

## 🔄 任务重发流程

```mermaid
flowchart TD
    subgraph 后端重发逻辑
        S0[步骤0: 查询子任务\nservice_url/device_code/area_id] --> S1
        S1[步骤1: 前置任务检查\n上一条是否执行中] -->|是| E1[阻断: PRECEDING_TASK_ACTIVE]
        S1 -->|否| S2[步骤2: 检查大模板状态\n排除6和8]
        S2 -->|不满足| E2[阻断: INVALID_STATUS]
        S2 -->|通过| S3[步骤3: 检查子模板状态\n排除6和8]
        S3 -->|不满足| E3[阻断: INVALID_STATUS]
        S3 -->|通过| S4[步骤4: 预占设备\nPOST /enableAGV]
        S4 -->|失败| E4[阻断: PREEMPT_FAILED]
        S4 -->|成功| S5[步骤5: 取消任务\nPOST /ics/out/task/cancelTask]
        S5 -->|失败| E5[阻断: CANCEL_FAILED]
        S5 -->|成功| S6[步骤6: 预占设备\nPOST /enableAGV 第二次]
        S6 -->|失败| E6[阻断: PREEMPT_FAILED]
        S6 -->|成功| S7[步骤7: 生成新sub_order_id\n子ID+1]
        S7 --> S8[步骤8: 修改数据库\n大模板→5 子模板→5]
        S8 --> OK[返回成功]
    end

    subgraph 前端渐进刷新
        OK --> F1[2s后刷新]
        F1 --> F2{状态还是5?}
        F2 -->|是| F3[3s后刷新]
        F2 -->|否| F_end[停止刷新]
        F3 --> F4{状态还是5?}
        F4 -->|是| F5[4s后刷新]
        F4 -->|否| F_end
        F5 --> F_end
    end
```

## 📁 相关文件

| 文件 | 说明 |
|------|------|
| `templates/query/unified_home.html` | 前端页面（JS逻辑 + HTML模板） |
| `routes/task_routes.py` | 后端 API 路由（设备查询、任务查询、重发等） |
| `modules/query/task_query_extended.py` | 查询核心逻辑（重发、设备状态、数据补充等） |
| `skill/skill_query_page.md` | 开发维护指导 |
